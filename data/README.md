# data/ - 数据文件夹

存放抓取到的原始数据和处理后的数据。

## 子目录

### raw/ - 原始数据
直接从网站抓取的原始内容，不做任何修改。
按日期组织，例如 `raw/2026-07-02/`。

### processed/ - 处理后数据
经过清洗、格式化后的结构化数据（JSON/CSV），供日报生成使用。
按日期组织，例如 `processed/2026-07-02/`。
